[project]
name = "ren2024-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "crsec-tools",
]

[tool.uv.workspace]
members = ["tools"]

[tool.uv.sources]
crsec-tools = { workspace = true }
